{% extends 'base/site.html' %}
{% block title %}
	{{app.name}}
	{{app.version}}
{% endblock %}
{% block page %}
{% from 'macros/macros.twig' import pagination %}
<div class="border-b border-gray-300 dark:border-gray-700 text-2xl py-4 mb-4">Showing messages for {{ckey}}
</div>
{% set url = url_for('tgdb.player.messages',{ckey:ckey}) %}
{{pagination(pagination.pages, pagination.currentPage, url)}}
  {% include 'messages/listing.twig' %}
{{pagination(pagination.pages, pagination.currentPage, url)}}
  <div id="secret_message_note" class="hidden">
    <strong class="font-bold text-xl">Secret Note</strong>
    <hr class="mb-2">
    This note is secret, and should not be shared with, or disclosed to non-admins
  </div>
{% endblock %}
