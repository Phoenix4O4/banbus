{% from 'macros/macros.twig' import userBadge %}
{% if ticket.isBwoink %}
{{userBadge(ticket.sender)}} bwoinked {{userBadge(ticket.recipient)}}
{% elseif 'Ticket Opened' == ticket.action %}
{{userBadge(ticket.sender)}} Opened Ticket
{% else %}
{{userBadge(ticket.sender)}} {{ticket.action_label}} {{userBadge(ticket.recipient)}}
{% endif %}
<time class="tooltip">{{ticket.timestamp}}</time>