{% from 'macros/macros.twig' import userBadge %}
{% if ticket.isBwoink %}
{{userBadge(ticket.sender, 'text-tiny')}} bwoinked {{userBadge(ticket.recipient, 'text-tiny')}}
{% elseif 'Ticket Opened' == ticket.action %}
{{userBadge(ticket.sender, 'text-tiny')}} Opened Ticket
{% else %}
{{userBadge(ticket.sender, 'text-tiny')}} {{ticket.action_label}} {{userBadge(ticket.recipient, 'text-tiny')}}
{% endif %} <time class="tooltip">{{ticket.timestamp}}</time>